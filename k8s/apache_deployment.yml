apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache
  namespace: grupo7a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
        - name: apache
#          image: zoltar238/kubeapache:1.0
          image: fmoha21/pps-apache:${FINAL_IMAGE_TAG_WAF}
          ports:
            - containerPort: 80

