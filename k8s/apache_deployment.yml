apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache-waf
  namespace: grupoa7
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache-waf
  template:
    metadata:
      labels:
        app: apache-waf
    spec:
      containers:
        - name: apache
          image: zoltar238/kubeapache:1.0
          ports:
            - containerPort: 80
