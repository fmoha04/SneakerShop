apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-initdb
  namespace: grupo7a
data:
  init.sql: |-
    CREATE DATABASE IF NOT EXISTS ciberA7;
    USE ciberA7;

    CREATE TABLE zapatos(
        id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
        nombre VARCHAR(255) NOT NULL,
        descripcion VARCHAR(255) NOT NULL,
        precio DECIMAL(9,2) NOT NULL,
        precio_iva DECIMAL(9,2),
        foto VARCHAR(255),
        marca VARCHAR(255)
    );

    CREATE TABLE comentarios(
        id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
        usuario VARCHAR(255) NOT NULL,
        descripcion VARCHAR(255) NOT NULL
    );

    CREATE TABLE usuarios(
        usuario VARCHAR(100) NOT NULL PRIMARY KEY,
        clave VARCHAR(255) NOT NULL,
        perfil VARCHAR(100) NOT NULL
    );

    INSERT INTO zapatos (nombre, descripcion, precio, precio_iva, foto, marca) VALUES 
    ('UltraBoost 22', 'Amortiguacion superior para corredores de asfalto.', 160.00, 193.60, 'ultraboost_22.jpg', 'Adidas'),
    ('Classic Leather', 'Estilo retro y minimalista en cuero blanco.', 85.50, 103.45, 'classic_leather.jpg', 'Reebok'),
    ('Old Skool', 'Zapatillas de lona iconicas para skate.', 70.00, 84.70, 'vans_oldskool.jpg', 'Vans'),
    ('Chuck Taylor All Star', 'Bota alta clasica de lona.', 65.00, 78.65, 'converse_allstar.jpg', 'Converse');

    INSERT INTO comentarios (usuario, descripcion) VALUES
    ('admin@N1', 'Revisar el margen de beneficio de las zapatillas Vans el próximo mes.'),
    ('admin@N2', 'Error corregido en la visualización de la foto de las Converse.');

    INSERT INTO usuarios (usuario, clave, perfil) VALUES
    ('root', '1234', 'admin'),
    ('admin@N1', 'trynexttime!', 'admin'),
    ('admin@N2', 'tryhackme!', 'admin'),
    ('soporte_tecnico', 'dev_access_01', 'admin');
