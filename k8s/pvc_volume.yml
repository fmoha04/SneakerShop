apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grupoa7-pvc
  namespace: grupoa7
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
